@Model List<MVCCrud.Models.ViewModels.ListaTablaViewModel>
@{
    ViewBag.Title = "Mis datos";
}

<h2>ViewBag.Title</h2>

<div class="row">
    <div class="col-md-12">
        <table>
            <tr>
                <th>#</th>
                <th>Nombre</th>
                <th>Correo</th>
            </tr>
            @foreach(var oElemento in Model)
            {

            }
        </table>
    </div>
</div>
